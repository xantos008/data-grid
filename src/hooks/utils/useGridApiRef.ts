import { useGridApiRef as useCommunityGridApiRef } from '@mui/x-data-grid';
import { GridApiExtra } from '../../models/gridApiExtra';

export const useGridApiRef = useCommunityGridApiRef<GridApiExtra>;
